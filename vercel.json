{
    "builds": [{
        "src": "cricket_analytics/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "500mb" ,"runtime": "python3.9"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "cricket_analytics/wsgi.py"
        }
    ]
}